# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/nickle-scheduler?Unicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 123456
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
# 自定义配置
nickle:
  scheduler:
    schedule-time: 10*1000
logging:
  level:
    nickle.scheduler.server.mapper: INFO